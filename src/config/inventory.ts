/** Stock level at or below which a product is flagged "low stock" — storefront badges and the
 *  admin low-stock report must agree on this number, so it lives in one place. */
export const LOW_STOCK_THRESHOLD = 3;
